diff --git a/src/models/RootStore.ts b/src/models/RootStore.ts
index c0fbe9a1..3845248a 100644
--- a/src/models/RootStore.ts
+++ b/src/models/RootStore.ts
@@ -79,6 +79,26 @@ const projectsData: Record<string, ProjectData> = {
 
 type RequestToRefresh = (nextResolve: boolean) => void;
 
+const enableAutoLauncherUpdating = () => {
+    let isStartApp = false,
+        setStartApp = () => null;
+
+    const updateStartApp = () => setStartApp(isStartApp);
+
+    (
+        (window.require || (() => null))('electron')?.ipcRenderer || {on: () => null}
+    ).on('checkautolauncher', (event, data) => {
+        isStartApp = data?.isStartApp;
+        updateStartApp();
+    });
+
+    return store => {
+        setStartApp = store.setStartApp || (() => null);
+        updateStartApp();
+    };
+
+};
+
 export const RootStore = types
     .model('RootStore', {
         isGodMode: types.optional(types.boolean, isGodMode()),
@@ -237,18 +257,25 @@ export const RootStore = types
             });
         };
 
+        let setSoftphoneStore = () => null;
+
         const afterCreate = () => {
             initialState = getSnapshot(self);
 
             setHighchartsOptions();
             setHttpRequestsDefault();
+            setSoftphoneStore = enableAutoLauncherUpdating();
         };
 
         const resetStore = () => {
             applySnapshot(self, initialState);
         };
 
-        return { afterCreate, resetStore };
+        return {
+            afterCreate,
+            resetStore,
+            setSoftphoneStore: store => setSoftphoneStore(store) 
+        };
     });
 
 export type DependencyInjection = {
