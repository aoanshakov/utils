tests.addTest(options => {
    const {Tester} = options;

    describe('', function() {
        let tester;

        beforeEach(function() {
            tester = new Tester(options);
        });

        it('', function() {
        });
    });
});
