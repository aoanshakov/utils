user root;

events {}

http {
    include mime.types;

    server {
        location / {
            root /usr/local/src/html/top;
        }

        location /public {
            root /usr/local/src;
        }
    }
}
